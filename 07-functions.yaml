# Functions in Ansible
- name: Converting into Uppercase
  hosts: localhost
  connection: local
  vars: 
    Course: "Devops"
  tasks:
    - name: Convert into Uppercase
      ansible.builtin.debug:
      msg: "{{ Course | upper }}"